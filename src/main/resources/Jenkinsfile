@Library('demogroovy')_
pipeline {
    agent any
    stages {
	
        stage ('Example') {
            steps {
                // demo.info 'Starting' 
                script { 
                    info 'Starting'
                    warning 'Nothing to do!'
                }
            }
        }
    }
}